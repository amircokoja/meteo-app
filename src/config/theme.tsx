import { createTheme } from "@mui/material";

const THEME = createTheme({
  typography: {
    fontFamily: `"Mukta", "Helvetica", "Arial", sans-serif`,
    allVariants: {
      color: "#333333",
    },
  },
});

export default THEME;
